import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Thuốc tim mạch',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
